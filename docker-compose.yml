version: "3.8"

services:
  ner-api:
    build: .
    container_name: fastapi-ner-api
    ports:
      - "80:8000"
    restart: unless-stopped
    volumes:
      - huggingface_cache:/root/.cache/huggingface

volumes:
  huggingface_cache:
    name: ner_model_cache
